# OCR assets ARE committed (extension/lib/tesseract/) so the extension is
# self-contained and installs offline on any machine. Do not ignore them.

# Build artifacts / packaging
dist/

# Node (only needed to regenerate icons)
node_modules/

# OS
Thumbs.db
.DS_Store
